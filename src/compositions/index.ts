import { default as RemotionRoot } from "./the-hook";
import { default as RemotionRoot } from "./the-secret-rise-of-quiet-vacationing-0-00-0-30";

export const compositions = {
  RemotionRoot,
  RemotionRoot,
};

export default compositions;
