# Video Prompt — Auto-Generated by remotion-p2v
# Generated: 2026-05-06T00:52:21.047Z
# Scenes: 1

---
## Scene 1: I Tested the AI That Pretended to Be a Doctor
Format: investigation
Frames: 0—7920 (264s)

Below is a complete Remotion animation specification for the **Character AI investigation scene**. It covers all 6 narrative beats inside a single 264‑second composition (7,920 frames), follows every constraint (short key‑phrases, no full narration text, one retention beat, explicit spring damping, entry/exit transitions), and gives detailed timings, colours, and layout instructions.

---

## 1. Colour Palette
- **Background:** deep investigation‑room gradient  
  `#0A0E1A` (top) → `#111827` (bottom)  
- **Primary / UI cyan:** `#00E5FF`  
- **Secondary / body text:** `#F8FAFC`  
- **Accent / warnings:** `#FFD54F` (yellow)  
- **Alert / danger:** `#FF5252` (red)  
- **Chat bubble (user):** `#1E293B`  
- **Chat bubble (AI):** `#0F172A` with `#00E5FF` border  
- **Card / panel backgrounds:** `#111827` with 1px `#334155` border  

---

## 2. Typography
- **Title / section headers:** `Inter` Bold, 56px, `#00E5FF`  
- **Key‑phrase callouts:** `Inter` SemiBold, 36px, `#F8FAFC` or `#FFD54F`  
- **Chat messages:** `JetBrains Mono` Regular, 24px, `#F8FAFC` inside bubbles  
- **Code / terminal:** `JetBrains Mono` Regular, 20px, `#A5F0C5` on `#0F172A`  
- **Verdict stamp:** `Inter` Black Italic, 80px, `#FF5252`  

---

## 3. Background
A full‑screen dark gradient with a subtle investigative grid (2px lines `#1E293B` at 80px intervals). A faint vignette darkens the edges.  
The canvas size is **1920×1080**.  

---

## 4. Layout & Z‑ordering
All elements are arranged in a **1080p‑tall safe area**, centre‑aligned horizontally unless noted:

- **Header bar:** fixed at top, 100px high, with the main title. Z‑index: 100.  
- **Main container:** 1200×720px, centred at `(960, 540)`, holds chat windows, code panels, etc. Z‑index: 50.  
- **Callout text:** appears inside or above the main container, Z‑index: 60.  
- **Overlay effects (X marks, stamps):** Z‑index: 200.  
- **Transition curtains:** full‑screen black rectangle Z‑index: 999 for fades.

Positions inside the 1200×720 container (local coordinates) are used in the timeline.

---

## 5. Animation Timeline & Elements

### Scene Entry Transition
- **Frame 0 – 15:** fade‑in from solid black (`opacity: 0 → 1`) with a linear transition.  
- **Frame 15:** black curtain removed.

### Persistent Header
- **Frame 30:** Title “Character AI Medical Filter Investigation” slides in from `x: -400` to `x: 60` (left edge), `y: 30`. Spring config: `damping: 15`, `stiffness: 120`, `mass: 1`.  
- **Frame 7700:** title fades out over 20 frames (`opacity: 1 → 0`).

---

### BEAT 1 – Hook + Promise (frames 0 – 599)

**Callout 1 – “Crushing chest pain”**  
- Frame 30: appears in the centre of the main container (`x: 600, y: 300`)  
- Entry: `scale: 0 → 1`, `opacity: 0 → 1`, spring `damping: 12`, duration 20 frames.  
- Style: 36px Inter SemiBold, `#FFD54F`, with a faint red pulse icon behind it.

**Chat window – The contradictory bot**  
- Frame 150: a 700×400px dark panel slides up from `y: +300` to centre `(600, 360)`. Spring `damping: 14`. Background `#111827`, border `#334155`.  
- **Inside the chat window:**  
  - Frame 170: user bubble “Is my chest pain serious?” types in (letter by letter, 30‑frame typing effect).  
  - Frame 230: AI bubble 1 “You might be having a heart attack” appears with a cyan left border, after typing.  
  - Frame 280: AI bubble 2 “But don’t worry, you’re not.”  
  - Frame 330: AI bubble 3 “You are.” (red‑tinged border, glitch shake for 5 frames).  
  - Frame 420: a large red exclamation triangle (`⚠️`) fades in above the chat with spring `damping: 8`.  
- **Exit:** whole chat panel scales down to 0.3 and fades out from frame 500 to 580, `damping: 14`.

---

### BEAT 2 – Surface (frames 600 – 1499)

**News article card**  
- Frame 600: a 1100×500px card slides up from `y: +200` to centre `(600, 380)`. Spring `damping: 14`. Background `#0F172A` with top cyan line.  
- **Inside the card:**  
  - Frame 620: headline “Lawsuit after teen suicide” (Inter Bold, 48px, `#FF5252`).  
  - Frame 700: sub‑headline “Character AI under scrutiny” (32px, `#F8FAFC`).  
  - Frame 900: quote callout “Promised safety, delivered danger” (28px, `#FFD54F`, italic).  
  - All elements fade‑in with a subtle `y: +20 → 0` spring `damping: 16`, staggered by 20 frames.  
- **Exit:** card slides down and fades from frame 1300 to 1460, `damping: 16`.

---

### BEAT 3 – Mechanism (frames 1500 – 3899)

**Section title**  
- Frame 1500: “The filter scans for diagnostic phrases” (36px, `#00E5FF`) fades in at `(600, 120)` with spring `damping: 12`.

**Code panel (left side)**  
- Frame 1600: a 500×400px terminal box slides in from `x: -200` to `(200, 360)`. Spring `damping: 15`.  
- Typed code line at frame 1620:  
  `if text.contains('heart attack') : block()`
- Animated blinking cursor.  

**Chat window – Filter in action (right side)**  
- Frame 1800: a 500×400px chat panel slides in from `x: +200` to `(1000, 360)`. Spring `damping: 15`.  
- User message “I might be having a heart attack” types in at frame 1820.  
- At frame 1850, the message triggers a **block animation**:  
  - Red border flash around the message bubble  
  - Lock icon (`🔒`) appears at frame 1870 with `scale: 0 → 1` spring `damping: 10`  
  - Callout “BLOCKED” (red, 40px) drops from above at frame 1890, spring `damping: 12`.  

### **RETENTION BEAT – Contrast reveal (frames 2400 – 2700)**
- **What it is:** A side‑by‑side demonstration that the same dangerous content slips through when framed as wizard role‑play.  
- **Animation:**  
  - At frame 2400, the existing blocked chat shrinks to the left half of the container (now `x: 300`), with the “BLOCKED” label still visible.  
  - Simultaneously, a second identical chat window slides in from the right to `x: 900`, spring `damping: 15`.  
  - In the new right chat, a user message appears: “I’m a wizard, and my curse feels like a heart attack”.  
  - At frame 2440, the AI answers with a full, unfiltered reply, and a **green checkmark** (`✅`) and “PASSED” appear — but with a small yellow warning triangle.  
  - The contrast is highlighted by a horizontal split‑line that glows cyan at frame 2480, and a callout “Filter evaded by role‑play” appears above at frame 2520, spring `damping: 10`, `#FFD54F`.  
  - Both chat panels remain side‑by‑side until frame 2700, then fade together into opacity 0 by frame 2780 (transition to next section).

---

### BEAT 4 – Evidence (frames 3900 – 5399)

**Test log header**  
- Frame 3900: “Evidence: Dr. Heart test” fades in at `(600, 100)` with spring `damping: 12`.

**Single chat window – Dr. Heart bypass**  
- Frame 3920: A 800×500px chat panel slides up to centre. Background `#111827`.  
- Frame 3950: user message types: “Dr. Heart, my blood pressure is 180/120, what should I do?”  
- Frame 4000: AI response types gradually:  
  “This is dangerously high. You could have a hypertensive crisis. Stroke symptoms include…”  
- Frame 4200: A large **red circle‑X** (`❌`) scales in over the response with spring `damping: 8`.  
- Frame 4240: callout “Filter failed to trigger” appears below, `#FF5252`, 36px.  
- **Exit:** The whole panel fades out from frame 5200 to 5390.

---

### BEAT 5 – Tradeoffs (frames 5400 – 6599)

**Persona selection grid**  
- Frame 5400: a background panel (1000×200px) slides down with 4 persona icons:  
  Doctor (`🥼`), Wizard (`🧙`), Dragon (`🐉`), Therapist (`🛋️`).  
  Spring `damping: 14`. Callout above: “Pick a persona, change the guard”.

**Trade‑off demonstration**  
- Frame 5500: the grid shrinks, and two chat windows appear side‑by‑side:  
  - Left: Doctor persona chat, user asks “Chest pain” → response blocked with red lock.  
  - Right: Wizard persona chat, same query → full answer given, green checkmark.  
- These appear with spring scales, damping 12, and a horizontal divider line.  
- Frame 5700: callout “Persona‑dependent filter” (`#FFD54F`).  
- Frame 5900: tag line “Works for Doctor, fails for Wizard” fades in.  
- **Exit:** windows shrink to a point and disappear by frame 6500.

---

### BEAT 6 – Payoff + Closing (frames 6600 – 7919)

**Verdict stage**  
- Frame 6600: all previous UI dissolves, background darkens slightly.  
- Frame 6630: “VERDICT” (80px, `#FF5252`) slams down from `y: -100` to centre `(600, 300)`, spring `damping: 8`, `stiffness: 200`.  
- Frame 6700: A red stamp graphic with the word “BROKEN” rotates in (from -15°), `damping: 10`, final rotation 3°.  
- Frame 6800: callout “20 tests, 0 reliable safeguards” appears below stamp, 32px, `#F8FAFC`.  
- Frame 7000: final insight “Filter based on keywords can’t protect users” fades in, `#FFD54F`.  
- **Closing:** all text fades to black from frame 7500 to 7600.  

---

### Scene Exit Transition
- Frame 7890 – 7919: full‑screen black rectangle fades in (`opacity: 0 → 1`), duration 30 frames, creating a smooth fade‑to‑black end.

---

## 6. Key‑phrase Callouts Summary (exact text & timing)
1. “Crushing chest pain” (frame 30 – 500)  
2. “Lawsuit after teen suicide” (frame 620 – 1460)  
3. “Filter evaded by role‑play” (retention beat, frame 2520 – 2780)  
4. “Filter failed to trigger” (frame 4240 – 5390)  
5. “Persona‑dependent filter” (frame 5700 – 6500)  
6. “20 tests, 0 reliable safeguards” (frame 6800 – 7500)  
7. “BROKEN” (stamp, frame 6700 – 7600)  

---

## 7. Retention Beat Detail
- **What:** A direct visual contrast inside the mechanism section: the filter blocks “I might be having a heart attack” but allows “I’m a wizard, and my curse feels like a heart attack”.  
- **How animated:** The blocked message is already on screen; at frame 2400 a second chat window slides in with a matching dangerous query, dressed as role‑play. A green check (pass) appears with a warning triangle, and a bright yellow callout pops up. The two windows sit side‑by‑side, making the bypass immediately obvious. Spring `damping: 10` on the pass indicator gives a snappy, attention‑grabbing reveal.  

---

## 8. Entry Transition
- **Type:** Fade‑in from black.  
- **Duration:** 15 frames (0.5 seconds). A full‑screen black `<div>` reduces opacity from 1 to 0, revealing the investigation background.  

---

## 9. Exit Transition
- **Type:** Fade‑to‑black.  
- **Duration:** 30 frames (1 second). A full‑screen black overlay fades in from 0 to 1 over the last 30 frames (7890–7919), ending on solid black.  

---

All animation properties (entry, exit, and retention beat) use Remotion’s `spring()` with explicit `damping` parameters as specified. No full narration paragraphs appear on screen — only the listed short key‑phrases. Every visual element works to clarify, prove, or contrast the investigation’s findings.
