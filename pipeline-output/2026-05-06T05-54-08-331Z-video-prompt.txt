# Video Prompt — Auto-Generated by remotion-p2v
# Generated: 2026-05-06T05:54:08.331Z
# Scenes: 1

---
## Scene 1: The Genius Launch Trick
Format: explainer
Frames: 0—6744 (224.79999999999998s)

**Remotion Explainer Scene — Full Composition (6744 frames)**  
This is a single, continuous composition that animates the entire 6-beat narrative about Meg Stalter’s music career strategy. Every visual element is functional — it clarifies, proves, compares, or emphasizes. No narration text is displayed on screen; only short 3–6 word key phrases appear.

---

### 1. Color Palette
- **Background gradient:** `linear-gradient(135deg, #0B0C10 0%, #1A1A2E 100%)`
- **Primary (coral):** `#FF5E57`
- **Secondary (cyan):** `#45E3FF`
- **Accent (gold):** `#FFD166`
- **Text:** `#FFFFFF` with a subtle `text-shadow: 0 0 20px rgba(0,0,0,0.8)`

---

### 2. Typography
- **Headings / major callouts:** `'Poppins', sans-serif`, weight `700`, size `64px`, all-caps, letter-spacing `0.05em`
- **Sub‑callouts & supporting short text:** `'Poppins', sans-serif`, weight `600`, size `40px`, all-caps
- **Small statistical labels or footnotes (when needed):** `'Inter', sans-serif`, weight `500`, size `24px`, uppercase tracking

---

### 3. Background
A subtly moving radial gradient that slowly shifts its center over the full duration. It starts with the brightest area near the top‑center and drifts gently, giving a feeling of constant energy without distraction. No decorative elements.

---

### 4. Layout & Z‑ordering
All content is center‑aligned horizontally within a `1920×1080` viewport. Elements appear in a vertical stack, layered by z‑index:
- `z‑100`: main key‑phrase text
- `z‑50`: diagrams, charts, icons
- `z‑10`: animated decorative lines or accent shapes (strictly functional)
- `z‑5`: the background gradient

---

### 5. Entry & Exit Transitions
- **Entry (frames 0–30):** full‑screen black fades to the background gradient (opacity `0 → 1` over 30 frames).
- **Exit (frames 6700–6744):** all elements fade to black over 44 frames; the last frame is solid black.

---

### 6. Animation Timeline & Narrative Beats

#### **BEAT 1 – HOOK + PROMISE (frames 30–1200)**
*Narration: “Meg Stalter didn’t launch her music career with a press release. She launched it…”*

| Frame(s) | Element | Entry Animation | Behavior & Exit |
|----------|---------|-----------------|-----------------|
| 30–70 | `.bg-gradient` | Fade‑in (30f) complete |
| 50–90 | **Key phrase: `MEG STALTER`** (center, z‑100) | `scale` spring from `0.5 → 1`, damping `12`, stiffness `150`, fade‑in | Remains until 1150 |
| 80–120 | **Key phrase: `NO PRESS RELEASE.`** (below name, z‑100) | `scale` spring from `0.5 → 1`, damping `10`, stiffness `160` + slide‑up 30px | Remains until 1150 |
| 130–300 | **“Press release” paper icon** (white rectangle with fake lines) slides in from the left edge | `translateX(-1920 → 0)` spring damping `14`, then stops at center | Sits still until 200 |
| 200–350 | **Paper tear & reveal** – The icon splits into two jagged halves that fly apart left/right (`translateX` ±500, fade). A glowing **video‑play button** (coral, `#FF5E57`) is revealed at center. | Tear: halves spring‑apart damping `10`. Play button: `scale` from `0 → 1.2 → 1` (damping `8`, stiffness `200`) | Play button pulses (scale oscillation `1 ↔ 1.03`) until 1150 |
| 350–1150 | Subtle pulsing of play button + faint aura | `transform: scale` sine loop | All Beat 1 elements start fading out from frame 1150 to 1200 (opacity 1→0) |

---

#### **BEAT 2 – SURFACE (frames 1200–2200)**
*Narration: “Attention is the most expensive resource on earth right now. Traditional music l…”*

| Frame(s) | Element | Entry Animation | Behavior & Exit |
|----------|---------|-----------------|-----------------|
| 1200–1240 | **Key phrase: `ATTENTION`** (giant, 96px, center) | `scale` spring `0.3 → 1`, damping `12`, stiffness `140` | Holds |
| 1240–1270 | **Sub‑callout: `MOST EXPENSIVE RESOURCE`** (below, 40px) | Slide‑up 40px + fade‑in, spring damping `14` | Holds |
| 1280–1500 | **Price‑tag graphic:** a cyan line drops from top‑center, a gold circle with “$” appears, then a counter spins up from `0` to `$7,300,000` (frames 1300‑1450, jump‑easing with snap). | Line: `scaleY` from 0, damping `15`. Counter: number spring‑change with `useSpring` | Becomes static |
| 1500–2000 | **Secondary callout: `TRADITIONAL LABELS SPEND MILLIONS`** fades in, accompanied by a small downward‑pointing arrow and a tiny, flat‑heartbeat monitor (flatlining). | Fade‑in + subtle tilt blur, damping `18` | Holds until 2150 |
| 2000–2200 | Transition – price tag and arrow fade out, background gradient shifts hue slightly (to prepare for mechanism) | Opacity 1→0 over 40 frames | Beat 2 ends |

---

#### **BEAT 3 – MECHANISM (frames 2200–3300)**
*Narration: “Underneath, the mechanism was ruthlessly clever. Between each contest round, her…”*

| Frame(s) | Element | Entry Animation | Behavior & Exit |
|----------|---------|-----------------|-----------------|
| 2200–2240 | **Key phrase: `THE CLEVER MECHANISM`** | `scale` spring `0 → 1`, damping `12` | Holds |
| 2240–2500 | **Feedback loop diagram** – A central `MEG` circle (coral) surrounded by three orbiting nodes: `FANS`, `MEMES`, `REMIXES`. Arrows connect them in a continuous loop. All elements build one by one. | Nodes: `scale` spring (damping 12) with staggered delays (60 frame gaps). Arrows: drawn using `stroke-dashoffset` spring from 1 to 0 (damping 15) | Loop stays until 3250 |
| 2500–2700 | **Key phrase: `FANS BECOME CREATORS`** pops in at top‑right of loop | `translateY(20) → 0`, damping `10`, with a small scale bounce | Stays |
| 2700–2850 | **Key phrase: `THE MEME ENGINE`** pops at bottom‑left | Same spring entry | Stays |
| 2850–3300 | The entire loop gently spins (`rotate` 360° over 15s, constant) while the key phrases pulse occasionally | | At frame 3250, all Beat 3 elements begin fading out over 50 frames |

---

#### **BEAT 4 – EVIDENCE (frames 3300–4500)**  
*RETENTION BEAT IS HERE*
*Narration: “Here’s the evidence. When an artist drops a single through a traditional PR push…”*

| Frame(s) | Element | Entry Animation | Behavior & Exit |
|----------|---------|-----------------|-----------------|
| 3300–3340 | **Key phrase: `THE EVIDENCE`** | `scale` spring damping `11` | Holds |
| 3350–3500 | **Side‑by‑side comparison bars** – Left: small, dull bar labelled `TRADITIONAL PR` (4% height). Right: a massive coral bar labelled `MEG’S VIRAL` that **spring‑grows from 0 to 92% height** with heavy overshoot (damping `8`, stiffness `220`) | Left bar enters with a simple fade‑slide, right bar uses the aggressive spring | The massive bar smashes through a faint ceiling line that cracks on impact |
| 3500–3600 | **Key phrase: `3,400% MORE STREAMS`** slams onto the screen between the bars | `scale` from `2 → 1`, damping `10`, stiffness `180`, with a brief white flash overlay | Holds |
| **3600–3900 RETENTION BEAT** | **1) Micro‑question `BUT WHY?`** appears in cyan, blinking twice. **2) Sudden reveal: `ALGORITHMIC AMPLIFICATION`** with a lightning‑bolt icon zapping the growth bar, causing golden spark particles | `BUT WHY?` fades in (3600), blinks (3640‑3680), then spins out and is replaced by the lightning bolt (scale 0→1, damping 7). The phrase fades in with a harsh glitch effect | High‑impact surprise — the “why” is instantly answered |
| 3900–4400 | A small “Algorithmic flywheel” graphic appears below — a wheel with `STREAMS ↠ PLAYLISTS ↠ ALGORITHM ↠ MORE STREAMS` that rotates | Spring‑spin entry, then continuous slow rotation | At 4400, all evidence elements start fading out over 50 frames |

---

#### **BEAT 5 – TRADEOFFS (frames 4500–5600)**  
*Narration: “Now, this doesn’t work for everyone. It works if you already have a strong, visual…”*

| Frame(s) | Element | Entry Animation | Behavior & Exit |
|----------|---------|-----------------|-----------------|
| 4500–4540 | **Key phrase: `THE TRADEOFFS`** | `scale` spring damping `12` | Holds |
| 4540–4700 | **Warning triangle icon + `NOT FOR EVERYONE`** fade‑in below | Triangle: `scale` spring damping `9`, text damping `14` | Holds |
| 4700–5100 | Three checklist items slide in sequentially (80 frame gaps): **`NEEDS STRONG PERSONA`**, **`REQUIRES EXISTING AUDIENCE`**, **`BACKLASH RISK`** | Each slides from right (translateX 300 → 0) with spring damping `15` | Stay in place |
| 5100–5500 | **Scale balancing visual** – A seesaw with a polished “safe weight on one side and a chaotic “meme” weight on the other. The meme weight slowly tips down, shaking. | Tilt: `rotate` spring damping `6` (slow, heavy), shake via `translateX` oscillation | The seesaw stabilizes at an angle, then fades out at 5550 |
| 5500–5600 | All tradeoff elements fade out over 50 frames | | |

---

#### **BEAT 6 – PAYOFF + CLOSING (frames 5600–6700)**  
*Narration: “Here’s my final judgment. Polished rollouts are flatlining. The artists who win …”*

| Frame(s) | Element | Entry Animation | Behavior & Exit |
|----------|---------|-----------------|-----------------|
| 5600–5650 | **Key phrase: `POLISHED ROLLOUTS FLATLINE`** accompanied by a flat‑heartbeat line that appears then goes flat | Line draws (`stroke‑dashoffset`), text scales in (damping 11) | Holds |
| 5650–5750 | **`THE ARTISTS WHO WIN…`** fades in (ellipsis …) | Simple fade, no scale | Sits |
| 5750–5850 | **`…ARE MESSY`** slams down in giant coral text, bouncing once | `translateY(-100 → 0)` spring damping `8`, stiffness `220`, overshoot | Stays |
| 5850–6100 | A stylized **`MEG STALTER`** signature with a crown icon fades in, gently pulsing | `scale` + fade, damping `14` | Pulsing |
| 6100–6650 | **Final callout: `EMBRACE THE CHAOS`** in gold, surrounded by floating gold particles that move slowly | Text: `scale` spring, particles: `opacity` and `translate` spring trails | Screen holds |
| 6650–6700 | All elements begin fading out | Opacity 1→0 over 50 frames | Leads to exit transition |

---

### 7. Retention Beat — Summary
The retention beat occurs during the **Evidence** segment (frame 3600‑3900). A micro‑question `BUT WHY?` appears, then is instantly reframed with the explosive reveal `ALGORITHMIC AMPLIFICATION` — a lightning bolt zaps the comparison chart, showing that the massive difference comes from the algorithmic flywheel, not just virality. This moment of contrast and instant explanation is the animated hook that keeps the viewer engaged.

---

### 8. Visual Notes for Implementation
- All `scale` springs use `transform: scale()`, with `useSpring` configured with explicit `damping` and `stiffness` as noted.
- The loop diagram in the Mechanism beat can be built with `<Circle>`, `<Line>`, and `<Arrow>` components, animated using `useSpring` for path drawing.
- The spark particles on the retention beat can be a simple `<Particles>` component with a burst triggered by a spring‑driven opacity.
- Ensure layer ordering matches z‑index values so text always stays on top of diagrams.
- No narration text is ever placed; the key‑phrase callouts are the only on‑screen text.

This composition tells the full story from hook to payoff with a single, fluid Remotion <Composition>, using spring physics to make every insight land with clarity and energy.
